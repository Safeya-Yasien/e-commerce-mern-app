export interface ICustomer {
  id: string;
  firstName: string;
  lastName: string;
  fullName: string;
  email: string;
  age: number;
  phone: string;
  country: string;
  gender: string;

  // i will remove it later
  createdAt: string;
  // lastUpdated: string;
}
